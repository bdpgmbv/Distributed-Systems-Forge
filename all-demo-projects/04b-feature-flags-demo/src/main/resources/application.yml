server.port: 8080
spring:
  application.name: feature-flags-demo
  datasource: { url: "jdbc:h2:mem:flags", driver-class-name: org.h2.Driver }
  jpa: { hibernate.ddl-auto: create, show-sql: true }
  h2.console: { enabled: true, path: /h2 }
logging.level.com.demo: DEBUG
