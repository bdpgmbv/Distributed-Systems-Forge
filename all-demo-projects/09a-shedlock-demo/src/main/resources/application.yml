server.port: 8080
spring:
  application.name: shedlock-demo
  datasource: { url: "jdbc:h2:mem:shedlock", driver-class-name: org.h2.Driver }
  jpa.hibernate.ddl-auto: create
  sql.init.mode: always
logging.level.com.demo: DEBUG