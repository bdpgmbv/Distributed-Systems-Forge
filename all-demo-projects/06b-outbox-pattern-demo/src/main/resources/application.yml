server.port: 8080
spring:
  application.name: outbox-pattern-demo
  datasource: { url: "jdbc:h2:mem:outbox", driver-class-name: org.h2.Driver }
  jpa: { hibernate.ddl-auto: create }
  h2.console: { enabled: true, path: /h2 }
logging.level.com.demo: DEBUG
